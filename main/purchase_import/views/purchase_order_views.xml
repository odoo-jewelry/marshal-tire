<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="purchase_order_form" model="ir.ui.view">
        <field name="name">purchase.order.form.inherit.line.import</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header/button[@id='draft_confirm']" position="after">
                <button name="action_open_line_import"
                        type="object"
                        string="Import Lines"
                        groups="purchase.group_purchase_user"
                        invisible="state != 'draft' or locked or not partner_id or order_line"/>
            </xpath>
        </field>
    </record>
</odoo>
