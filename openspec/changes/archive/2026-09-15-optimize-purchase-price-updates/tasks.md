## 1. Исходное поведение и измерения

- [x] 1.1 Зафиксировать до изменения кода воспроизводимые серверные замеры на тестовом заказе из 266 строк: одна изменённая цена, все реально изменённые цены, повторный Fill, устаревшие снимки при уже актуальных товарах. Использовать одинакового пользователя и режим кеша, не менее пяти повторов с откатом; сохранить время, число SQL-запросов и записей ORM.

## 2. Обновление снимков без лишних записей — C1

- [x] 2.1 В `_refresh_price_snapshots` сохранить полный обход подходящих строк и прежний набор результатов; пропускать запись только при совпадении полного сохраняемого состояния с учётом точности полей, инициализации и обоих полей ручного переопределения. Для пропуска сохранить штатные проверки записи строк и полей.
- [x] 2.2 Добавить профильные тесты: одна запись снимка вместо 266, ноль записей при повторном Fill, обновление устаревших и общих соседних снимков, сохранение ручной цены при Update, сброс через Fill и инициализация при нулевых значениях. Проверить счётчики записи бизнес-полей и итоговые значения.

## 3. Последовательное применение только изменившихся цен — C2

- [x] 3.1 В `_apply_price_update_payload` сохранить фиксированные цели, порядок кандидатов и выбор последнего кандидата стоимости; исключать только поля, уже совпадающие с текущим состоянием на данном шаге. Сохранить обычные обратные вычисления цены варианта и проверку доступа на пропускаемом пути, включая делегированную цену шаблона.
- [x] 3.2 Добавить тесты полностью совпадающего товара при устаревшем снимке, изменения только одного поля, общей цены вариантов с разными доплатами, последовательности A → B → A и граничной точности стоимости/валюты/наценки. Проверить отсутствие лишних записей товара и истории стоимости.

## 4. Доступ, атомарность и совместимость

- [x] 4.1 Проверить запрет действий при ограничениях на модель, запись и поле для пропускаемых цен/снимков; смешанный пакет с последующей ошибкой должен откатывать все изменения. Подтвердить прежний ранний выход без кандидатов.
- [x] 4.2 Выполнить существующие профильные тесты и добавить недостающее покрытие пакета заказов разных компаний: изоляция стоимости, FIFO/AVCO, штатная история и партии Standard Price, состояния заказа, копирование, обычное сохранение и отсутствие автоматического обновления цен при действиях жизненного цикла.

## 5. Проверка ускорения и безопасного обновления

- [x] 5.1 Повторить четыре замера из 1.1 после реализации; сопоставить медианы времени, число запросов и записей, подтвердить критерии из design.md. Разобрать регрессии на массовых реальных изменениях сверх разброса измерений. Отдельно указать неизмеренную задержку браузера/сети.
- [x] 5.2 Обновить версию модуля, сохранив пользовательские изменения манифеста. Проверить синтаксис/импорт, штатное обновление модуля и отсутствие изменения рабочих значений при обновлении на разрешённой тестовой базе; миграция данных не требуется. Не устанавливать зависимости и не запускать браузерные тесты; недоступные проверки явно перечислить.
- [x] 5.3 Проверить соответствие реализации всем 13 сценариям, записать результаты проверки и фактические трудозатраты. Подготовить изменение к отдельной синхронизации/архивации; при последующей синхронизации канонической спецификации пересобрать документацию штатным генератором.
