## 1. Проверить выбранное преобразование на изолированных данных

- [x] 1.1 На тестовой базе проверить смену идентичности выполненных `stock.move`/`stock.move.line` без повторного проведения: даты, количества, значения и связанные документы сохраняются, стандартные остатки пересчитываются согласованно.
- [x] 1.2 Проверить вывод доказанной старой пары в неоперационное состояние с сохранением свидетельств: стандартные количественные отчёты, `is_in`/`is_out` и FIFO не учитывают ни одну сторону. Зафиксировать результат и при несовместимости пересмотреть design и estimate до основной реализации.

## 2. Группа объединения и проверка всей истории — K2

- [x] 2.1 Расширить анализ на архивный дубль и полную группу старых источников одного основного товара; сохранить запреты циклов, несовместимости и неподдерживаемых цепочек.
- [x] 2.2 Собрать явный план всех связанных складских, закупочных, продажных и POS-записей, известных возвратных и проектных ссылок. Непокрытые операционные связи блокируют всю группу с указанием записей.
- [x] 2.3 Проверять компанию всей истории, финансовые и аналитические связи, закрытые периоды, резервы, открытые движения, инвентаризации и количественную сверку независимо от текущего остатка.
- [x] 2.4 Добавить серверные проверки допустимого профиля, архивного источника, нескольких старых источников, нулевого остатка и полного отказа при одном неподдерживаемом источнике.

## 3. Аудит, права и транзакционная защита — K6

- [x] 3.1 Создать защищённые постоянные операции и строки аудита с ACL и правилами компании; связать старую/новую идентичность и свидетельства пар, сохранив предыдущие снимки корректировок и пересчётов.
- [x] 3.2 Реализовать закрытый контракт проверенного преобразования, блокировки в стабильном порядке и контроль ревизии состава записей, включая конкурентное создание новых строк. Подготовленные ранее пересчёты должны становиться устаревшими.
- [x] 3.3 Реализовать атомарное применение, повторный возврат результата уже применённой операции и запрет подделки, копирования как применённой, изменения и удаления завершённого аудита.
- [x] 3.4 Проверить серверными тестами права, компании, подделку контекста/RPC, конкурентные изменения и вставки, откат после поздней ошибки и успешный повтор после неё.

## 4. Перенос исходной идентичности и физических остатков — K3, K4

- [x] 4.1 Реализовать проверенные обработчики товарных ссылок реальных движений, детализации и связанных поддерживаемых документов, сохранив идентификаторы, состояния, даты, количества, описания, цены, скидки, налоги и суммы.
- [x] 4.2 Согласовать узкие разрешённые изменения с защитами исторических списаний и корректировок; неизменяемые прежние свидетельства связывать через аудит. Непокрытые защищённые случаи отклонять до записи.
- [x] 4.3 Объединить остатки по полному ключу поддерживаемых измерений без новых движений; обеспечить нулевой остаток дубля и сверку основной карточки с суммой прежних физических остатков.
- [x] 4.4 Проверить исходные приходы 20+100, разные стоимости поступлений, коммерческие инварианты, количественные измерения, разрешённые связи возврата и отказ на непокрытых связях. Проверить сохранение стоимости реальных расходов до ручного пересчёта.

## 5. Преобразование старых служебных пар — K5

- [x] 5.1 По существующим ссылкам происхождения найти и проверить все старые пары группы; отказывать при неполных, несогласованных или защищённых связях.
- [x] 5.2 Реализовать проверенный в разделе 1 способ исключения пар из активных остатков и оценки с сохранением их исходных данных и связи с аудитом; реальные поступления и продажи не пересоздавать.
- [x] 5.3 Проверить пример «приход 100, продажа 4, перенос 96», несколько старых источников и повторный вызов; текущий суммарный остаток после конверсии сохраняется, служебная пара не участвует в стандартном FIFO.

## 6. Выбор двух способов в существующем мастере — K1

- [x] 6.1 Добавить выбор «Перенос текущего остатка» / «Полное объединение истории» в существующий мастер, вход полной конверсии из архивного товара, обзор группы/документов/пар/ограничений и соответствующее режиму подтверждение. Смена режима аннулирует прежний просмотр.
- [x] 6.2 Сохранить прежний перенос текущего остатка в интерфейсе и публичном сервисе, включая вызовы без параметра режима; новый путь включать только явно. Сохранить псевдонимы, конфигурацию и архивирование; добавить русские подписи и доступ к аудиту.
- [x] 6.3 Проверить оба режима, смену режима после просмотра, отмену, прежний API, новый дубль после полного объединения в каждом режиме, карты без складской истории и импорт по прежним кодам.

## 7. Историческое списание по объединённой истории — K7

- [x] 7.1 Заменить общий запрет объединённых товаров проверкой доказанной полной конверсии; для старых непреобразованных групп показывать конкретный путь исправления.
- [x] 7.2 Проверить проведение 31 шт. на 31 августа после объединения приходов 20+100 на 28 августа, включая отдельное служебное время. Сохранить отказы при дефиците тогда или позже, резервах и финансовых ограничениях; выручка, платежи, долги и смены не меняются.

## 8. Ручной пересчёт объединённой истории — K8

- [x] 8.1 Расширить существующий инструмент отбором по применённым объединениям и действием из их аудита; определять все основные товары, раннюю границу реальных расходов и фиксированный конец предпросмотра.
- [x] 8.2 Для отбора по историческим списаниям учитывать более раннюю границу затронутого объединения и объяснять её в предпросмотре; не разрешать сужение обычными фильтрами или режимом только нулевых стоимостей.
- [x] 8.3 Переиспользовать стандартный FIFO, существующий план «склад → POS», аудит и защиту устаревшего просмотра. Исключать выведенные пары, обрабатывать ненулевые значения и складской план без POS; не запускать пересчёт автоматически и не вводить признаки ожидания.
- [x] 8.4 Проверить разную стоимость исходных приходов, затронутую продажу до даты исторического списания, последующие ненулевые значения, отсутствие POS-строк, неподдерживаемый источник, устаревший просмотр и сохранение обычных режимов пересчёта.

## 9. Обновление и итоговая проверка

- [x] 9.1 Обновить зависимости интеграции, версии и порядок загрузки прав/представлений; проверить установку через `tire`. Обновление модулей не преобразует старые движения и товарные ссылки автоматически.
- [x] 9.2 Запустить серверные тесты затронутых модулей и обновление на `odoo_tire_testing` с явной конфигурацией и отдельным портом. Сверить стандартные отчёты и историю нескольких источников на репрезентативных изолированных данных; записать результаты и ограничения.
- [ ] 9.3 Проверить компиляцию XML и вручную пройти мастер из активной/архивной карточки, аудит и ручной пересчёт. Браузерные автоматизированные тесты не создавать и не запускать.
- [x] 9.4 После реализации согласовать и синхронизировать три delta-спецификации с учётом активного `add-pos-historical-stock-writeoff`, сохранить требования обоих способов с явными границами режимов и перестроить документацию. Отметить необходимость ручного пересмотра существующих карточек функций, не переписывая их автоматически.
