## 1. Адресное подтверждение безопасного механизма — D1/D2 в C3/C4

Статус 15.09.2026: после согласования пользователем реализован общий контроль
версий источников. D1/D2 проверены независимыми транзакциями и серверными
тестами. Результаты и ограничения — [discovery.md](discovery.md).

- [x] 1.1 Выполнить D1 на `odoo_tire_testing`: закрепить характеристическим тестом Standard Price 0 → поступление → продажа 0 → FIFO и позднее поступление 900; подтвердить фиксированную запись по старому источнику, отсутствие изменений количества/дат/receipt/product costs/учётных записей и устойчивость при `_set_value()`/`product.value`.
- [x] 1.2 Завершить D1 проверкой обычного возврата после ремонта, влияния на стандартные отчёты и ранней точки запрета удаления модуля с историей. Если требуются бухгалтерская корректировка или новый движок оценки, остановить зависимую реализацию и пересмотреть проект.
- [x] 1.3 Выполнить D2 независимыми транзакциями: согласованный порядок блокировок, известные и новые движения/возвраты/финансовые ссылки, уже начатый writer и writer после захвата блокировки. Зафиксировать доказанный протокол и обновить design/estimate до зависимой реализации при изменении подхода.

## 2. Анализ допуска и доказательство стоимости — C2

- [x] 2.1 Реализовать пакетный анализ прямых POS-источников и условий допуска: zero/done/FIFO/periodic, компания, складируемость, tracking/ownership/структура, закрытая смена и отсутствие финансовых/проектных зависимостей. Определять заблокированный период штатными правилами; недостаточные сведения не считать разрешением.
- [x] 2.2 Реализовать доказательство единственного строго предшествующего supplier receipt, совпадения единиц/компании/места и достаточности количества после предыдущих расходов. Не использовать сегодняшние остатки как историческое основание; проверять отсутствие неоднозначных перемещений и полной истории, не обрезая её молча.
- [x] 2.3 Собрать уникальный план по stock.move, проверить полный охват зависимых POS-строк в frozen selection, рассчитать фиксированную сумму из receipt quantity/value и сохранить предупреждение о текущем снимке/неокончательной закупочной оценке. Неприменимые группы показывать целиком с причиной.
- [x] 2.4 Покрыть R02–R09/R14 и обычный S37: позднее поступление, предыдущий расход, одинаковые даты, несколько receipts, нулевой/нечисловой источник, общий источник смены, частичный выбор, периоды, счета/проводки/аналитика, компании и неподдерживаемые структуры.

## 3. Режим, просмотр и подтверждение — C1

- [x] 3.1 Добавить отключённый по умолчанию режим, складскую детализацию с отношением один issue — одна запись и ссылками от POS-деталей. Изменение режима сбрасывает просмотр/подтверждение; сохранить прежние снимки и поведение обычного режима.
- [x] 3.2 Расширить существующую форму таблицей складского плана, источниками, раздельными stock/POS итогами и отдельным подтверждением изменения исторической оценки; подготовить английские исходные строки и русский перевод. Использовать прежние создание/отмену и историю операций.
- [x] 3.3 Проверить R01/R10–R13 и S30: просмотр без операционных записей, итог 814,46, отсутствие двойного счёта движений, разные валюты/даты, обязательные причина и stock acknowledgement, недопустимость подмены подтверждением нуля, отмена без последствий.

## 4. Фиксированная оценка и стандартные последующие действия — C3

- [x] 4.1 Реализовать подтверждённый в D1 приватный сервис фиксированной ORM-записи stock value и защищённую связь движения с историей. Не вызывать текущий FIFO или перепроведение, не создавать движений/проводок и не записывать quants.
- [x] 4.2 Добавить узкое batch-совместимое наследование стандартной оценки для отремонтированных движений: сохранить значение при безопасном повторе, отклонять несовместимое изменение, не оставлять ложный product.value при отказе. Для всех обычных движений сохранить стандартный путь.
- [x] 4.3 Проверить R18/R23/R25 и S31/S38: позднее поступление не подменяет сумму, повтор не даёт дрейфа, будущий возврат использует исправленный источник, последующая смена receipt value не меняет историю автоматически, обычные stock/POS-сценарии сохранены.

## 5. Единое применение и конкуренция — C4

- [x] 5.1 Встроить stock plan в существующий `action_apply()` с протоколом D2, перечитыванием входов/состава истории и отказом при внешнем устаревании. Авторизованные собственные stock-изменения отделить от внешнего изменения снимка.
- [x] 5.2 Атомарно записать уникальные складские значения, вызвать стандартный POS-расчёт выбранных строк, проверить совпадение с просмотром и сохранить фактический результат. Сохранить идемпотентность done, смешанный просмотренный набор и запрет новых пропусков во время применения.
- [x] 5.3 Проверить R15/R16/R19/R24 и S25–S29/S36: ошибка после первого stock write с полным откатом, конфликт двух операций и обычного пересчёта, повтор после потерянного ответа, изменение курса/receipt/history/финансовых зависимостей, стандартная маржа и отсутствие воздействия на невыбранные строки.

## 6. Доступ, неизменяемость и совместимость — C5

- [x] 6.1 Добавить ACL и правила компаний новой модели; проверять пересечение POS manager/stock manager только в новом режиме и обычные права на каждую запись. Защитить историю/ссылки от create/write/unlink/import/RPC/One2many и поддельного контекста.
- [x] 6.2 Реализовать безопасные значения новых полей, копию только критериев без результатов/подтверждений и ранний запрет удаления модуля с применёнными ремонтами по проверенной точке D1. Зарегистрировать модели/представления, явный stock_account и корректный порядок данных; повысить версию без перезаписи чужих изменений.
- [x] 6.3 Проверить R17/R20–R22/R26/R27/S39: обычный POS manager без складских прав, stock manager без POS-прав, ограничения записей/полей/компаний, обходы прямыми запросами, старые ready/done/draft операции, копирование, обновление и удаление с/без применённой истории.

## 7. Итоговая проверка и передача

- [x] 7.1 Выполнить Python/XML/ACL/manifest/external-ID проверки, установку/повторное обновление `pos_cost_recompute` на разрешённой тестовой базе с фактическим addons_path и отдельным портом 8077; запустить профильные серверные тесты нового режима, базового модуля и выбранную стандартную FIFO/POS-регрессию. Не устанавливать отсутствующие зависимости или инструменты.
- [x] 7.2 Проверить просмотр и применение более 1000 POS-строк и анализ более 10 000 исторических движений без фиксированного предела или обрезания выборки; зафиксировать стоимость анализа. При доступности вручную проверить режим/просмотр/подтверждение; браузерную автоматизацию не создавать, непроверенное перечислить.
- [x] 7.3 Сверить результат со всеми 38 сценариями, записать результаты D1/D2, ограничения и фактические проверки. Перед последующей синхронизацией обеспечить базовую спецификацию `pos-cost-recompute`, затем применить обе delta и пересобрать документацию. Синхронизация/архивирование и применение к рабочим данным выполняются отдельно от этой реализации.
