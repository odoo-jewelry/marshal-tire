## 1. Суммарная себестоимость в стандартном отчете

- [x] 1.1 В `tire` добавить наследование `report.pos.order`: поле `total_cost` только для чтения с суммированием и расширение `_select()` через `super()` по формуле из `design.md`. Сохранить стандартную кратность строк, соединения, фильтры и прочие показатели.
- [x] 1.2 Подключить модель, добавить перевод `Cost` → «Себестоимость» и повысить версию `tire`. Сохранить имеющиеся посторонние изменения. Использовать стандартный автоматический выбор числовых мер без изменения начальных настроек отчета.

## 2. Серверные проверки общего механизма

- [x] 2.1 Добавить тесты фактического SQL-представления и ORM-агрегации: суммы и группировки, фильтрация, количество без повторного умножения, частичный возврат, курс 2 и нулевой/отсутствующий курс, неполные и отсутствующие затраты (S01, S03–S06). В тех же данных проверить отсутствие дублирования при нескольких платежах и категориях.
- [x] 2.2 Проверить отражение изменения сохраненных затрат и повторное чтение без побочных изменений; стандартные ограничения компаний и доступа; отмену с обычным фильтром и без него; сохранение прежних показателей (S07, S09–S12).
- [x] 2.3 Проверить метаданные поля и перевод для стандартного меню мер сводной таблицы и диаграммы, сохранить начальные активные меры и настройки действия (S01–S02, S12). Не создавать браузерных тестов.

## 3. Обновление и итоговая проверка

- [x] 3.1 Выполнить статические проверки Python, импортов, перевода, версии и зависимостей манифеста. Подтвердить отсутствие новых XML/data/security-записей и необходимости миграции данных.
- [x] 3.2 Прочитать необходимые настройки `/etc/odoo/odoo.conf`, использовать фактический `addons_path` и разрешенную тестовую базу с суффиксом `_testing`. Обновить `tire` на порту 8077 или следующем свободном и проверить доступность SQL-столбца и сохранность исторических затрат/статусов (S08). Не устанавливать недостающие зависимости.
- [x] 3.3 Запустить новые серверные тесты и стандартные тесты `TestReportPoSOrder`; проверить отчет через ORM. Зафиксировать выполненные проверки, результат обновления и ограничения, включая отсутствие автоматической проверки браузерного поведения.


## Результаты проверки — 2026-09-15

- Реализация: `tire` версии `19.0.1.4.0`, поле `total_cost`, суммирование сохраненной себестоимости с валютной формулой стандартной маржи. Новых зависимостей, XML-представлений, таблиц, прав доступа и миграций данных нет.
- Проверены синтаксис Python, импорты при загрузке Odoo, манифест, существующие представления и внешние идентификаторы, перевод через штатный загрузчик и `fields_get(lang='ru_RU')`, отсутствие ошибок форматирования изменений.
- На `odoo_tire_testing` успешно выполнены установка `tire`, обновление `tire,point_of_sale` с выбранными серверными тестами и отдельное обновление `tire` с сохраненными контрольными заказами.
- **11 тестов пройдены:** 7 новых `TestPosOrderReportCost` и 4 стандартных `TestReportPoSOrder`; 0 ошибок и 0 падений. Команда: `odoo --http-port=8077 -c /etc/odoo/odoo.conf -d odoo_tire_testing -u tire,point_of_sale --stop-after-init --test-enable --test-tags /tire:TestPosOrderReportCost,/point_of_sale:TestReportPoSOrder`. Журнал: `/tmp/pos_report_cost_tests.log`.
- Отдельная проверка обновления сохранила себестоимости 80 и −40, количество, цены, признаки завершенного расчета и состояния заказов. После обновления сумма себестоимости 40, маржа 20. Созданные для проверки заказы, сессия, конфигурация и товар удалены. Журналы: `/tmp/pos_report_cost_upgrade.log`, `/tmp/pos_report_cost_upgrade_verify.log`.
- Первая версия тестовых данных задавала курс до завершения стандартных вычислений заказа; тест исправлен, чтобы задавать сохраненный исторический курс после них. Производственная формула не менялась.
- Общая загрузка русского языка выявила независимую ошибку `AttributeError` в разборе существующего `main/pos_customer_debt/i18n/ru.po`. Файл не изменялся. Перевод новой меры успешно проверен отдельной загрузкой переводов `tire` в транзакции с откатом.
- Локальный игнорируемый Git файл `main/tire/i18n/tire.pot` содержал устаревший список полей: Odoo при его наличии исключал новую строку из перевода. В локальный шаблон добавлен `Cost`. В чистом checkout этого игнорируемого файла нет; если на целевой среде сохранен старый `tire.pot`, его следует обновить перед загрузкой переводов. В `ru.po` новая строка сохранена как обычное изменение репозитория.
- Рабочая база `odoo_tire` не обновлялась. Браузерные автоматические тесты не запускались по правилам проекта; фактическое отображение в браузере автоматически не проверено. Канонические спецификации и карточки возможностей не менялись, пересборка документации не требовалась.
