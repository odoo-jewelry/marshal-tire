## 1. Обратный расчёт плановой цены

- [x] 1.1 Сделать `planned_sale_price` редактируемым вычисляемым полем с batch-safe обратным методом и сохраняемым точным ручным переопределением, не изменяющим `current_markup`.
- [x] 1.2 Добавить серверную проверку пригодности и инициализации строки и атомарно отклонять недопустимый ручной ввод с переводимым сообщением об ошибке.
- [x] 1.3 Сохранять ручную цену при изменении базы и явно сбрасывать переопределение через `Fill Current Prices`, возвращая обычный автоматический расчёт.

## 2. Форма заказа закупки

- [x] 2.1 Обновить наследованную форму `purchase.order`, разрешив ввод `planned_sale_price` только для инициализированных подходящих товарных строк и добавив нужные технические поля без изменения действий `Update` и `Update All`.
- [x] 2.2 Обновить шаблон переводов и переводы модуля для пользовательского сообщения проверки.

## 3. Автоматизированная проверка поведения

- [x] 3.1 Добавить сфокусированный тест точного сохранения ручного значения `12` после flush/re-read и неизменности `current_markup`.
- [x] 3.2 Добавить тест сохранения ручной цены после изменения базы и возврата к автоматическому расчёту после `Fill Current Prices`.
- [x] 3.3 Добавить тесты недоступности полезного ручного ввода для неинициализированных и неподходящих строк.
- [x] 3.4 Проверить, что ручное редактирование не записывает цены или себестоимость товара, а сценарий без ручного ввода и явные действия обновления сохраняют прежнее поведение.

## 4. Обновление и итоговая валидация

- [x] 4.1 Выполнить проверки синтаксиса Python, XML, переводов и компиляции наследованного представления.
- [x] 4.2 Прочитать фактический `/etc/odoo/odoo.conf`, обновить `purchase_price_control` на явной базе разработки временным процессом Odoo с `--http-port=8077` или следующим свободным портом и запустить сфокусированный серверный набор тестов модуля.
- [x] 4.3 Зафиксировать, что отдельная миграция данных не требуется, существующие строки начинают без ручного переопределения, а поведение формы в браузере автоматически не проверялось согласно правилам проекта.
