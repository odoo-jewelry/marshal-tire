## 1. Данные товара и POS loader

- [x] 1.1 Добавить в `tire` необязательное переводимое поле `product.template.pos_short_name` и включить его в `_load_pos_data_fields()` через batch-safe вызов `super()`.
- [x] 1.2 Добавить поле в существующее узкое наследование формы товара и обновить `tire.pot`, `ru.po` и `uk.po` для новой метки.

## 2. Отображение в основном каталоге POS

- [x] 2.1 Добавить в `point_of_sale._assets_pos` узкое QWeb-наследование `point_of_sale.ProductScreen`, которое передаёт в основной `ProductCard` значение `pos_short_name` с fallback на штатное имя, не меняя общий `ProductCard`, корзину или receipt-шаблоны.
- [x] 2.2 Подключить новый XML asset и поднять версию `tire` для upgrade-safe доставки изменения.

## 3. Проверка и регрессии

- [x] 3.1 Добавить focused non-browser тест, который проверяет наличие `pos_short_name` в списке и результате серверной POS-загрузки и сохранение полного `name`.
- [x] 3.2 Проверить Python syntax/imports, XML, external IDs/XPath, manifest assets, i18n-файлы и отсутствие необходимости в новых ACL, record rules и миграции.
- [x] 3.3 Прочитать фактическую Odoo-конфигурацию, обновить `tire` на явной development-базе через временный HTTP-порт `8077` и запустить focused-тест.
- [x] 3.4 Выполнить ручной smoke-check: после перезагрузки POS-данных плитка показывает сокращение, а после его очистки — полное имя; обычный, электронный и basic receipt в обоих случаях используют полное имя с деталями варианта. Зафиксировать, что браузерное поведение не было покрыто автотестом.
