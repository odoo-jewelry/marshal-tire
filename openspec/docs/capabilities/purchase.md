<!-- GENERATED FROM OPENSPEC FEATURE CARDS. DO NOT EDIT. -->

# Закупки

[← Вся документация](README.md)

## Импорт строк заказа на закупку

Управляемый импорт строк из CSV/XLSX в пустой черновик запроса коммерческого предложения или заказа на закупку с проверкой товаров и цен поставщика.

**Использование:**

В форме заказа на закупку добавлена кнопка `Import Lines`, доступная пользователю закупок для пустого документа в состоянии draft с выбранным поставщиком. Стандартные поля формы не добавлялись, не изменялись и не удалялись; кнопка открывает модальный мастер `Import Purchase Lines`.

В мастере пользователь выбирает профиль, загружает CSV/XLSX, задаёт лист, строки заголовков и данных, параметры CSV и ограничения файла, настраивает обработку неизвестных товаров и сопоставляет колонки. Действия `Read File` и `Reload File` перестраивают данные, `Validate` показывает построчный preview с найденным или планируемым товаром, количеством, единицей измерения, ценой и ошибками, а `Import Lines` атомарно добавляет проверенные строки в исходном порядке.

В меню `Purchase > Configuration > Import Profiles` менеджер закупок создаёт company-scoped профили. В форме профиля доступны параметры CSV/XLSX, строки заголовка и данных, лимиты, правила для неизвестных товаров, defaults нового товара, вкладки `Column Mapping` и `Product Lookup`; из мастера профиль можно сохранить действием `Save as New Profile`.

Основные сценарии: повторное использование профиля для нового файла; импорт существующих товаров по коду поставщика, внутреннему коду или штрихкоду; создание самостоятельного товара либо dynamic-варианта; обновление точной цены поставщика; исправление ошибок после preview и повторная проверка. Непустой или недействующий draft-документ, неоднозначное сопоставление, несовместимые данные и устаревший preview блокируют операцию без частичных изменений.

[Полная спецификация](../../specs/purchase-order-line-import/spec.md)

---

## Контроль закупочных и продажных цен

Позволяет закупщикам видеть расчётные показатели цены и явно сохранять закупочную и продажную цены товара при подтверждении заказа.

**Использование:**

В `Purchase > Configuration > Settings` добавлен блок Price Control с полями наценки по умолчанию и шага округления продажной цены для каждой компании. Стандартные поля настроек не изменялись и не удалялись.

В форме запроса коммерческого предложения и заказа на закупку в товарных строках добавлены текущая справочная закупочная цена, текущая продажная цена, наценка, эффективная закупочная цена, плановая продажная цена и флажок `Save Prices`. При подтверждении заказа система сохраняет рассчитанные значения только для отмеченных товарных строк; секции, подсекции, заметки и авансовые строки не участвуют.

Основные сценарии: сравнение текущих и плановых цен до подтверждения; применение company-specific наценки и округления; сохранение цен выбранных строк при обычном подтверждении или переходе в To Approve; повторное применение после возврата заказа в draft. Неотмеченные строки сохраняют стандартное поведение Odoo, отмена заказа не откатывает уже сохранённые цены, а дубликат заказа очищает флажки.

[Полная спецификация](../../specs/purchase-price-control/spec.md)
